package View;

import java.util.regex.Matcher;

public class Login {
    private Matcher m;

    public void run() {
        while (Menu.getMenu() == Menu.LOGIN) {
            // to do
            break;
        }
    }
}
